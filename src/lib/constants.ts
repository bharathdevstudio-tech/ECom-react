// All values are in DOLLARS (product.price is already in dollars from rowToProduct)
export const FREE_SHIPPING_THRESHOLD = 100;  // $100 → free shipping
export const FLAT_SHIPPING = 12;             // $12 flat shipping rate
export const TAX_RATE = 0.08;               // 8% tax
